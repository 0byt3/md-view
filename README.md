# Markdown File Viewer

This is a multi-modal markdown file viewer. It has vim-style keyboard bindings for navigation and highlighting. It is not an editor, but it will re-render on file change.

It includes mermaid flow rendering and language syntax highlighting.
